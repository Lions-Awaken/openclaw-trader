# Archived Migrations

These migrations were originally in this repo but belong to the
Twilight Underground Supabase project (`uupmzaglafeiakamefit`), not
the openclaw-trader project (`vpollvsbtushbiapoflr`).

Kept here for reference. They should be managed in the
`twilight-underground` repository going forward.

## Files

- `20260319_streamsaber_fly_migration.sql` — Creates `tiktok_stream_events`
  table with 30-day retention. References `tiktok_accounts` and `users`
  tables that live in TU's database.
